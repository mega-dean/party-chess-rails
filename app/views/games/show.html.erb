<%= link_to("back", games_path) %>

<h1>Game <%= @player.game.id %></h1>

<h4>Playing as <%= @player.id %></h4>

<div>
  <%= button_to("quit", sessions_destroy_path, { method: :delete }) %>
</div>

<%= turbo_stream_from("game_board") %>
<%=
render partial: "games/board_grid", locals: {
  player: @player,
}
%>
